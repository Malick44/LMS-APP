package com.repository;

import com.model.ContentSection;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ContentSectionRepository extends JpaRepository<ContentSection, Integer> {
}